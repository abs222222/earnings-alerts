# Google Sheets Configuration
# The spreadsheet ID from the Google Sheet URL
# Example: https://docs.google.com/spreadsheets/d/SHEET_ID_HERE/edit
GOOGLE_SHEET_ID=your_google_sheet_id_here

# Path to Google service account credentials JSON file
# For service account auth (recommended for automated scripts)
GOOGLE_CREDENTIALS_PATH=./credentials/service-account.json

# Alternative: Path to OAuth token file
# For OAuth-based auth (uses user's Google account)
# GOOGLE_TOKEN_PATH=~/.claude/code-executor/google_token.json

# Sheet name/tab containing earnings data
SHEET_NAME=Earnings

# Email Configuration
# Comma-separated list of recipients (or use config/recipients.json)
# EMAIL_RECIPIENTS=user@example.com,another@example.com

# Optional: Override sender email (defaults to authenticated user)
# EMAIL_FROM=alerts@yourdomain.com

# Logging
LOG_LEVEL=info
